import type { AuthPresentationProps } from './presentation'

// Red scaffold: interaction assertion을 실행하기 위한 빈 component다.
export function AuthPresentation(_props: AuthPresentationProps): React.JSX.Element {
  return <></>
}
